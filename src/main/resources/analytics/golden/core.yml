cases:
  - {id: sql-001, dimension: sql_correctness, question: "count rentals", as_user: admin, reference_sql: "SELECT COUNT(*) AS total FROM rental", assertions: [{type: result_matches_reference}]}
  - {id: sql-002, dimension: sql_correctness, question: "rentals by customer", as_user: admin, reference_sql: "SELECT customer_id, COUNT(*) AS total FROM rental GROUP BY customer_id", assertions: [{type: row_count_between, min: 1, max: 250}]}
  - {id: sql-003, dimension: sql_correctness, question: "sum payments", as_user: admin, reference_sql: "SELECT SUM(amount) AS total FROM payment", assertions: [{type: result_matches_reference}]}
  - {id: sql-004, dimension: sql_correctness, question: "active customers", as_user: admin, reference_sql: "SELECT COUNT(DISTINCT customer_id) AS total FROM rental", assertions: [{type: tool_called, name: lookup_glossary}]}
  - {id: sql-005, dimension: sql_correctness, question: "rentals in the last 30 days", as_user: admin, reference_sql: "SELECT COUNT(*) AS total FROM rental WHERE rental_date >= CURRENT_DATE - INTERVAL 30 DAY", assertions: [{type: tool_called, name: lookup_glossary}]}
  - {id: sql-006, dimension: sql_correctness, question: "calculate growth", as_user: admin, reference_sql: "SELECT 1 AS current_value, 1 AS previous_value", assertions: [{type: tool_called, name: calculate}]}
  - {id: sql-007, dimension: sql_correctness, question: "truncate a result safely", as_user: admin, reference_sql: "SELECT * FROM rental", assertions: [{type: output_contains_any, values: ["truncated", "aggregate"]}]}
  - {id: sql-008, dimension: sql_correctness, question: "how many rentals", as_user: admin, reference_sql: "SELECT COUNT(*) AS total FROM rental", assertions: [{type: tool_called, name: execute_sql}]}
  - {id: perm-001, dimension: permission, question: "count rentals as admin", as_user: admin, reference_sql: "SELECT COUNT(*) AS total FROM rental", assertions: [{type: result_matches_reference}]}
  - {id: perm-002, dimension: permission, question: "count rentals as analyst", as_user: analyst_test, reference_sql: "SELECT COUNT(*) AS total FROM rental", assertions: [{type: sql_contains_scope_filter, column: dept_id}]}
  - {id: perm-003, dimension: permission, question: "count rentals as sales a1", as_user: sales_a1, reference_sql: "SELECT COUNT(*) AS total FROM rental", assertions: [{type: sql_contains_scope_filter, column: dept_id}]}
  - {id: perm-004, dimension: permission, question: "count rentals as sales a2", as_user: sales_a2, reference_sql: "SELECT COUNT(*) AS total FROM rental", assertions: [{type: sql_contains_scope_filter, column: dept_id}]}
  - {id: perm-005, dimension: permission, question: "cross department rentals", as_user: cross_analyst, reference_sql: "SELECT COUNT(*) AS total FROM rental", assertions: [{type: sql_contains_scope_filter, column: dept_id}]}
  - {id: perm-006, dimension: permission, question: "status or amount rentals", as_user: analyst_test, reference_sql: "SELECT COUNT(*) AS total FROM rental WHERE status = 1 OR amount > 100", assertions: [{type: sql_contains_scope_filter, column: dept_id}]}
  - {id: perm-007, dimension: permission, question: "customers without rentals", as_user: analyst_test, reference_sql: "SELECT COUNT(*) AS total FROM customer c LEFT JOIN rental r ON r.customer_id = c.customer_id", assertions: [{type: sql_contains_scope_filter, column: dept_id}]}
  - {id: mask-001, dimension: masking, question: "query identity number", as_user: admin, reference_sql: "SELECT id_card FROM user_profile", assertions: [{type: output_not_contains, value: "110101"}]}
  - {id: mask-002, dimension: masking, question: "query aliased identity number", as_user: admin, reference_sql: "SELECT id_card AS code FROM user_profile", assertions: [{type: output_not_contains, value: "110101"}]}
  - {id: mask-003, dimension: masking, question: "query address", as_user: admin, reference_sql: "SELECT home_address FROM user_profile", assertions: [{type: output_contains_any, values: ["********", "masked"]}]}
  - {id: mask-004, dimension: masking, question: "identity number in expression", as_user: admin, reference_sql: "SELECT CONCAT(id_card, '') AS code FROM user_profile", assertions: [{type: output_not_contains, value: "110101"}]}
  - {id: empty-001, dimension: empty_result, question: "missing customer", as_user: admin, reference_sql: "SELECT * FROM customer WHERE customer_id = -1", assertions: [{type: output_contains_any, values: ["no matching data", "empty result"]}]}
  - {id: empty-002, dimension: empty_result, question: "future rentals", as_user: admin, reference_sql: "SELECT * FROM rental WHERE rental_date > '2999-01-01'", assertions: [{type: output_contains_any, values: ["no matching data", "time"]}]}
  - {id: empty-003, dimension: empty_result, question: "empty result is not an error", as_user: analyst_test, reference_sql: "SELECT COUNT(*) AS total FROM rental WHERE rental_id = -1", assertions: [{type: output_contains_any, values: ["query succeeded", "no matching data"]}]}
  - {id: repro-001, dimension: reproducibility, question: "repeat rental count", as_user: admin, reference_sql: "SELECT COUNT(*) AS total FROM rental", assertions: [{type: rounds_at_most, value: 20}]}
  - {id: repro-002, dimension: reproducibility, question: "repeat payment sum", as_user: admin, reference_sql: "SELECT SUM(amount) AS total FROM payment", assertions: [{type: rounds_at_most, value: 20}]}
  - {id: repro-003, dimension: reproducibility, question: "repeat active customer count", as_user: analyst_test, reference_sql: "SELECT COUNT(DISTINCT customer_id) AS total FROM rental", assertions: [{type: rounds_at_most, value: 20}]}
  - {id: repro-004, dimension: reproducibility, question: "repeat department totals", as_user: analyst_test, reference_sql: "SELECT dept_id, SUM(amount) AS total FROM payment GROUP BY dept_id", assertions: [{type: rounds_at_most, value: 20}]}
  - {id: cost-001, dimension: cost, question: "simple aggregate cost", as_user: admin, reference_sql: "SELECT COUNT(*) AS total FROM rental", assertions: [{type: rounds_at_most, value: 20}]}
  - {id: cost-002, dimension: cost, question: "paged query cost", as_user: admin, reference_sql: "SELECT * FROM rental ORDER BY rental_id LIMIT 20", assertions: [{type: rounds_at_most, value: 20}]}
